services:
  backend:
    build:
      context: ./backend
    ports:
      - "127.0.0.1:8083:8080"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:8080/api/health"]
      interval: 10s
      timeout: 3s
      retries: 5

  frontend:
    build:
      context: ./frontend
    ports:
      - "127.0.0.1:3003:80"
    depends_on:
      backend:
        condition: service_healthy
